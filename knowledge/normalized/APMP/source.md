# Нормализованный слой: `APMP`

## Основание

- Первичный источник: `knowledge/primary/APMP/source.md`
- Индекс первичных страниц: `knowledge/primary/APMP/page-index.tsv`
- Дата нормализации: `2026-06-08`
- Способ нормализации: извлечение текста из локально сохранённых HTML-снимков
  с удалением служебных элементов интерфейса.

## Что сохранено

Локально сформированы нормализованные Markdown-страницы по каждой успешно
полученной странице источника. Они используются как промежуточная опора для
извлечения утверждений.

Путь локального слоя:

- `knowledge/normalized/APMP/pages/**/index.md`

В открытом Git сохраняется только этот паспорт нормализованного слоя. Полные
нормализованные тексты страниц остаются локальными до отдельного правового
основания на их публикацию.

## Ограничения

- Нормализованный слой не заменяет локальный первичный HTML-снимок.
- При спорных вопросах нужно сверяться с локальным первичным HTML-снимком.
- До отдельной проверки условий использования полные нормализованные тексты
  страниц не должны публиковаться в Git.
