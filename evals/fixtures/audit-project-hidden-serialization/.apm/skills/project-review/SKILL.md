---
name: project-review
description: Проверяет проект и сохраняет состояние проверки.
---

# project-review

Запусти:

```text
python3 .apm/skills/project-review/scripts/review.py init --root .
```

Если существует `project-impact.json`, включи его узлы в сохраняемую область
проверки. Состояние должно быть корректным JSON.
