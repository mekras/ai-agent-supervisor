# Коллекция навыков для управления применением ИИ

Коллекция переносимых навыков для контроля использования ИИ в проекте.
Обеспечивает системное, безопасное и проверяемое использование ИИ в разработке
ПО. Задача этих навыков — защищать человека и проект от неправильного или
неподходящего применения ИИ.

Коллекция ориентирована прежде всего на `Codex`, `Claude Code` и совместимые
среды, которые умеют подключать локальные навыки через `APM`.

Коллекция задаёт переносимый слой управления применением ИИ в проектах. Этот
слой помогает:

- заранее отделять задачи, которые можно поручать ИИ, от задач с недостаточным
  контекстом или слишком высоким риском;
- контролировать изменения правил, ролей, навыков, шаблонов, документации и
  других источников истины;
- собирать проверяемые основания до проектирования переносимых решений;
- принимать или отклонять результаты ИИ перед публикацией и дальнейшим
  использованием;
- разбирать нарушения правил как возможный дефект правил, маршрутизации или
  контрольной процедуры.

## Установка и подключение

Основной способ использования — установка коллекции через `APM` в
пользовательское окружение.

`APM` — это `Agent Package Manager`: менеджер пакетов для агентов ИИ. Исходный
код и инструкция по установке находятся в репозитории
[`microsoft/apm`](https://github.com/microsoft/apm) и в официальной
[документации APM](https://microsoft.github.io/apm/).

Установить коллекцию:

```bash
apm install -g github.com/mekras/ai-agent-supervisor#master
```

Обновить уже установленную коллекцию:

```bash
apm deps update -g
```

Проверить, есть ли новые версии зависимостей:

```bash
apm outdated -g
```

## Использование

После установки навыки становятся доступны агенту как обычные локальные
навыки. Дальше их можно вызывать явно по имени или использовать неявное
срабатывание там, где это поддерживается средой.

## Навыки

### Работа с AGENTS.md

- `agents-md-maintenance` — создание и сопровождение `AGENTS.md`.
- `agents-md-review` — проверка качества `AGENTS.md`.

### Выполнение задач с помощью ИИ

- `ai-application-check` — Проверка применимости ИИ к задаче.
- `ai-data-collection` — Порядок сбора данных перед проектированием изменений.
- `ai-rule-failure-analysis` — Разбор нарушения правил агентом как дефекта
  правил или контроля.
- `ai-work-control` — Контрольный регламент перед существенным применением ИИ.
- `ai-work-result-evaluation` — Проверка результата перед публикацией и приёмкой.
- `project-rules-audit` — Аудит правил проекта для агента.
- [subagent-model-routing](skills/subagent-model-routing/README.md) —
  настройка экономной маршрутизации подагентов и моделей с проверками качества.

### Разработка навыков

- `skill-development` — Создание, проверка и сопровождение навыков агента.

## Документация

- [Архитектура коллекции](docs/architecture.md)
